import React from 'react';

export default function SportTracker(props){
    return(
        <div className="custom-section-containers">
            <div className="custom-section-headers">Sport Tracker</div>
        </div>
    )
}